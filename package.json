{
  "name": "fiiniapi",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.5",
    "hono": "^4.6.9",
    "lru-cache": "^11.0.2",
    "wrangler": "^3.84.1"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}